description: Adds support for Markup Views to the project
build:
    plugins:
        - org.grails:grails-views-markup
dependencies:
    - scope: build
      coords: "org.grails.views:grails-views-gradle:5.5.0"
    - scope: compile
      coords: "org.grails.views:grails-views-markup"

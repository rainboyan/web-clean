description: Adds support for JSON Views to the project
build:
    plugins:
        - org.grails:grails-views-json
dependencies:
    - scope: build
      coords: "org.grails.views:grails-views-gradle:5.5.0-RC1"
    - scope: compile
      coords: "org.grails.views:grails-views-json"
    - scope: compile
      coords: "org.grails.views:grails-views-json-templates"
